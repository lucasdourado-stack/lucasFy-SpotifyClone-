
const TextButton = ({label}) => {
  return (
    <div>
            <button className='text-zinc-400 font-bold hover:text-white hover:scale-105 transition-all cursor-pointer'>{label}</button>
    </div>
  )
}

export default TextButton
